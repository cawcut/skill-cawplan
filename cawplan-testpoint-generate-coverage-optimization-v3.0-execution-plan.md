# cawplan-testpoint-generate 测试点覆盖率优化方案（v3.0 执行计划）

> 本文件只是**执行计划**，不改动任何代码或 Skill/reference 文件。所有行号已在当前基线（commit `13f5980` "Generate Testpoint Coverage optimization"，working tree clean）重新 grep 核实，不沿用上一轮分析中的行号。
>
> 上一轮（v2.1 → v2.3）已解决「识别到因素后有没有继续往下问分区」（深度不足），已落地并 commit。本轮四条改动解决的是另一个角度——「存疑清单是不是无痕黑洞」「静默跳过是不是无痕黑洞」「自查是不是左手查右手」「规则是否过载挤占注意力」——与 v2.3 不冲突、不重叠、互补。

---

## 一、总览

| 优先级 | 改动 | 一句话 | 对应机制 | 风险 |
|---|---|---|---|---|
| **P0** | A/B 轴「静默跳过」→「具名跳过」 | 判 A/B 轴不适用时，强制多写一句原因再跳过；**不是**可事后调取的留痕（原计划叫「留痕跳过」，动手前核实发现不成立，见 §2 P0 节），不进存疑清单、不给 SQA 看 | 静默跳过缺一步强制复查 | 低 |
| **P1** | 「防编造 vs 防少写」拆分收尾核对 | 核对上一轮 v2.3 §6.4 是否已把「宁少不宁多」类措辞全部拆干净；**结论：已完成，本轮不产生新改动** | 红线 0 被误读为「防多写」 | 无（核对性质，不改文件） |
| **P2** | §6 自查新增「以需求为锚点的开放式反查」 | 在固定清单核对之后，加一步丢开清单、直接重读需求+草稿找茬，仍一轮不循环、仍受红线 0 | 自查左手查右手 | 中 |
| **P3** | closure/存疑纪律补「方向唯一但未拍板→转生成」通道 | 方向没有歧义、只是没人拍板测不测的分区，直接生成方向性行并标注待确认，不再默认转存疑 | 存疑清单是无痕黑洞（另一半——「转正式行」通道缺失） | 中 |

**实施纪律**：四条改动**一次只做一条、每条单独 commit、改一条验一条**。不允许在同一个 commit 里合并两条以上改动——这是为了保证任何一条效果不佳时能干净回退，不牵连其他三条。P0/P1 独立性最强、先做；P2/P3 涉及生成量与自查行为的变化，观察窗口更长，放在后面，且**建议在 P0/P1 观察结果稳定后才启动**。

---

## 二、逐条改动详述

### P0 — A/B 轴「静默跳过」→「具名跳过」（原计划名「留痕跳过」，已按下方核实结论改名）

**⚠️ 先澄清「留痕」到底留在哪、能不能事后调出来看（动手前的必要核实）**

对当前基线做了一次专门核实：`SKILL.md` 第 566 行明确写着 `## Session state (**in-conversation only**)`，往下列的三类状态——① Binding（product_id/requirement_id/五字段快照/url）、② Work set（原稿 snapshot、touched-row marks、current drafts、archived stubs）、③ Write（pending_write、write_outcome）——**没有任何一类包含「六槎位记录 / P1/P2 分区 / 轴判断理由」这类内部工作稿**。全文搜索「内部工作稿」（第 186、190 行）只找到「记一行、留在内部工作稿、不作为正式字段呈现给 SQA」这样的**指令**，但从未定义这份工作稿是一个被系统保留、可事后调取的持久化实体。

**结论：「内部工作稿」在当前实现里没有任何持久化机制，它只存在于该次生成的推理过程 / 对话上下文里，会话结束或上下文被压缩后不保证能被人翻出来看；即便在当次会话内，SQA 或审计者也没有任何指令要求或渠道把它调出来看。它是否偶然可见（例如宿主环境展示了 extended thinking），取决于运行环境，不受本 skill 控制，不能作为设计依据。**

**据此重新定位 P0 的真实价值**：P0 的价值**不是**「可回溯留痕」，而是「强制 AI 在下判断前多写一句理由，从而减少拍脑袋误判」——这是纯粹的书写降低草率效应，不依赖任何持久化或事后可查性。这个价值仍然成立，P0 仍然值得做，但下方的改动说明与验证方法已按此重新表述，不再使用「留痕」「可回溯」这类暗示持久化的措辞。

**改哪个文件的哪一段**：

1. `skills/cawplan-testpoint-generate/SKILL.md`，第 200 行（§5「Axis traversal」下的三态纪律列表，A/B 组一行）
2. `skills/cawplan-testpoint-generate/references/coverage-dimensions.md`，第 30 行（§二「使用定位与三条纪律」第 2 条，A/B 组一行）
3. `skills/cawplan-testpoint-generate/references/coverage-dimensions.md`，第 36 行（§二「三条纪律」第 3 条，自查用于暴露疑点，A/B 轴一行）

**before → after**

**① `SKILL.md:200`**

before:
```
- **A/B 组变化轴**（`输入类型` / `交互反馈` / `状态迁移` / `角色权限` / `来源入口`）：维持原三态 — 笃定不适用 → **静默跳过**；拿不准 → 存疑清单。
```

after:
```
- **A/B 组变化轴**（`输入类型` / `交互反馈` / `状态迁移` / `角色权限` / `来源入口`）：维持原三态 — 笃定不适用 → **具名跳过**（判断前须先写出一句 `〔X 轴〕判不适用（原因：…）`，与六槎位记录同级，**不进存疑清单、不呈现给 SQA**；此句是否事后可查取决于运行环境，本 skill 不保证、不依赖其可查性）；拿不准 → 存疑清单。
```

**② `coverage-dimensions.md:30`**

before:
```
   - **A/B 组变化轴**：不要求每个功能覆盖所有轴。判定为**明确不适用**的轴 → **静默跳过**（不生成、不汇报）；判定**不确定**的轴 → 写入**存疑清单**交人确认。
```

after:
```
   - **A/B 组变化轴**：不要求每个功能覆盖所有轴。判定为**明确不适用**的轴 → **具名跳过**（判断前须先写一句原因，**不生成、不进存疑清单、不汇报给人**）；判定**不确定**的轴 → 写入**存疑清单**交人确认。
```

**③ `coverage-dimensions.md:36`**

before:
```
   - **A/B 轴**：判定明确不适用者静默；判定不确定者方在存疑清单中点明。
```

after:
```
   - **A/B 轴**：判定明确不适用者具名跳过（判断前须先写一句原因，不进存疑清单、不汇报）；判定不确定者方在存疑清单中点明。
```

**为什么这么改、解决哪条机制**：核实确认 A/B 轴「不适用」完全依赖 AI 一次性主观判断、判完即弃、没有任何强制说明理由的动作（诊断机制 2「静默跳过缺一步强制复查」成立）。**这条改动的机制是「强制书写」，不是「留痕」**——按上方核实结论，这句原因不保证被任何人事后调取，它的作用是在判断动作本身里插入一个「必须先说出理由才能跳过」的关卡，逼 AI 面对一次「你为什么觉得这轴不适用」的自我追问，而不是允许它无理由地把三态选项之一直接勾掉。这是「减少拍脑袋误判」的机制，不是「审计留痕」的机制——若宿主环境恰好保留了推理过程，这句话可以被人看到，但那是运行环境的偶然属性，不是本条改动依赖或承诺的东西。

**交叉引用影响**：

- 无步骤重排——这条只替换措辞，不新增/删除任何步骤或段落。
- 检查是否有其他地方引用「静默跳过」这个词并需要同步：已 grep 全文（见下方核实记录），第 194、196、198、234、235 行也出现「静默」，但这些分别是：
  - 194 行：轴三态定义总述（`不适用（静默跳过）`）——**需要同步改**为「不适用（具名跳过）」，否则和 200 行新措辞矛盾。
  - 196 行：泛指「A/B 明确不适用静默」——**需要同步改**为「具名跳过」。
  - 198 行：P2 展开追问「命中不到的静默跳过」——**这是分区层面的静默**，不是轴层面的三态判断，性质不同（分区归属检查 §5 step 2 已经堵了「分区消失」这个口子，P2 这里的「静默跳过」指的是「某个展开追问方向压根没命中」，不是「轴判不适用」）。**不改**，避免混淆两种不同的「静默」。
  - 234 行：closure 「All other axes」段「A/B 组：笃定不适用仍静默」——**需要同步改**为「具名跳过」，否则和 200 行冲突。
  - 235 行：同段 C/D 轴「正面证明不适用 → 静默」——**这是 C/D 轴既有机制，本轮不动**（C/D 轴本来就要求正面证明才能静默，门槛已经比 A/B 轴高，且这轮诊断的靶子是 A/B 轴的无门槛静默，不是 C/D）。

  **完整改动点清单（含本条新发现的 194/196/234 行）**：

  | 文件 | 行号 | 改动 |
  |---|---|---|
  | `SKILL.md` | 194 | `不适用（静默跳过）` → `不适用（具名跳过）` |
  | `SKILL.md` | 196 | `A/B 明确不适用静默` → `A/B 明确不适用具名跳过` |
  | `SKILL.md` | 200 | 见上① |
  | `SKILL.md` | 234 | `A/B 组：笃定不适用仍静默` → `A/B 组：笃定不适用仍具名跳过（判断前须先写原因，不进存疑）` |
  | `coverage-dimensions.md` | 30 | 见上② |
  | `coverage-dimensions.md` | 36 | 见上③ |

- 325 行（§6 自审）「A 层模式项笃定不适用 → silent」：这里的 silent 指的是 review-checklist A 层条目命中判断，与「轴三态」不是同一机制（A 层是自查清单条目，不是变化轴）。**本轮不动**，留给后续如果要统一措辞再评估，不在本条范围内。

**该条独立验证方法**（已按「不保证持久化」的核实结论重新表述，不再假设内部工作稿可事后调取）：

1. **可执行的抽查方式**：仅在生成过程的推理内容对当次会话可见时（取决于宿主环境，例如某些客户端展示 extended thinking），可以抽 3-5 个真实需求跑生成，人工翻当次可见的推理内容，检查每一条 A/B 轴判「不适用」是否确实带了一句原因。**这条检查不总是可执行——推理过程不可见时，无法验证「是否写了原因」这件事本身，只能验证下一条。**
2. **不依赖内部工作稿可见性的检查（主要验证手段）**：核对 §7 呈现给 SQA 的输出（分节测试点表 + 存疑清单）里**没有**出现任何轴判断理由的文字——防止指令被误执行成「把这句理由写进存疑清单或呈现表」。这条不要求工作稿可见，可以稳定执行。
3. 对比改动前后同一需求的存疑清单行数：预期**不变**（这条改动不产生新的存疑条目，只是在判断动作前多加一道自我追问）。
4. **间接效果检查（替代「留痕可查」这个放弃的卖点）**：由于无法直接验证「AI 是否认真想了理由」，改用结果代理指标——对比改动前后，同一批需求里 A/B 轴被判「不适用」的比例是否下降、或存疑清单中 A/B 轴「拿不准」条目是否上升。如果比例几乎不变，说明「强制写一句理由」这个机关没有实际改变判断行为，只是空转的格式要求，此时应重新评估 P0 是否值得保留。

**若验证不过、如何单独回退**：这是四条里最容易回退的一条——6 处纯措辞替换，`git revert` 对应 commit 即可，不涉及新增/删除结构，不会留下孤立引用（因为改动没有新增编号、没有被其他章节引用其内部子步骤）。

---

### P1 — 「防编造 vs 防少写」拆分收尾核对

**结论先行：已完成，本轮不产生新改动。**

这条不是要新增改动，是要**核实上一轮 v2.3 §6.4 是否已经把「宁少不宁多」类收敛口号从「防少写」语境里拆干净、只保留在「防编造」（红线 0）语境**。核实方法：在当前基线重新 grep 一遍所有相关措辞。

**核实证据**（当前基线 `13f5980`，重新 grep 结果）：

```
$ grep -n "宁少不宁多\|宁可少生成\|force-generate\|Prefer fewer rows" \
    skills/cawplan-testpoint-generate/SKILL.md \
    skills/cawplan-testpoint-generate/references/review-checklist.md \
    skills/cawplan-testpoint-generate/references/coverage-dimensions.md

SKILL.md:246:- Before writing: would this assertion need a **number / literal message / threshold / error code**? If yes and not in the five fields → **do not generate** that part; 存疑 instead. **Prefer fewer rows over invented specifics.**
SKILL.md:248:- **「宁可少生成」** means fewer invented details — **not** skipping directional rows **or dropping behaviorally distinct partitions** when `coverage-dimensions.md` section 3 applies.
```

- `SKILL.md:246`：出现在**红线 0** 段落内，紧跟在「这条断言是否需要具体数字/文案/阈值/错误码，且五字段没给 → 不生成、转存疑」之后——语境是纯粹的「防编造」，措辞准确，**不应删**。
- `SKILL.md:248`：明确写「means fewer invented details — **not** skipping directional rows or dropping behaviorally distinct partitions」——这句话本身就是「拆分声明」，把「宁可少生成」的射程钉死在「防编造」上，同时明文排除了「跳过方向性行」和「丢弃行为分区」两种误读。**这正是本轮想要的效果，已经写好，不用再改。**
- `review-checklist.md:22`、`SKILL.md:325`、`SKILL.md:633`（Rules Index 冲突序）：已核实**不含**「宁少不宁多」字样，均已替换为「不编造具体值」/「不编造优先」。
- `coverage-dimensions.md` 全文：已核实**不含**任何「宁少」类措辞（该文件本来就没有这类表述，无需改动）。

**结论**：v2.3 §6.4「按调用点逐处替换收敛口号」的改动已经完整落地且经过本轮独立复核，8 个原调用点全部处理完毕，无遗漏、无残留。**本轮 P1 不产生任何文件改动**，此节仅作为审计记录留存。

**该条独立验证方法**：无需新验证——上面的 grep 输出即为验证结果，可在任意时刻重新跑一遍复核。

**若验证不过、如何单独回退**：不适用（本条不改动文件）。

---

### P2 — §6 自查新增「以需求为锚点的开放式反查」

**改哪个文件的哪一段**：`skills/cawplan-testpoint-generate/SKILL.md`，§6「Post-generation self-critique」，第 320-322 行之间（现有步骤 2 之后、步骤 3 之前插入新步骤）。

**当前 §6 步骤结构**（第 314-333 行）：

```
314  **Skip** when §5 step 8 triggered...
316  1. **Read the checklist file** ...
320  2. **Switch perspective** to 「资深测试评审」...
322  3. **Compare draft against each A-layer item**. On a hit: ...
327  4. **Hard rules** (non-negotiable): ...
333  5. **Scope**: run **once** before **first present of that round** ...
```

**before**（第 320-322 行）：
```
2. **Switch perspective** to 「资深测试评审」. Walk **A 层** items **in list order**, **one round only** — no multi-round loop. For each A-layer item, read **only the bold check line**; parenthetical maintainer notes are **not** required reading. **B 层** is capability-boundary awareness only — do **not** read B-layer items line by line.

3. **Compare draft against each A-layer item**. On a hit:
```

**after**（插入新步骤 3，原步骤 3/4/5 顺延为 4/5/6）：
```
2. **Switch perspective** to 「资深测试评审」. Walk **A 层** items **in list order**, **one round only** — no multi-round loop. For each A-layer item, read **only the bold check line**; parenthetical maintainer notes are **not** required reading. **B 层** is capability-boundary awareness only — do **not** read B-layer items line by line.

3. **Compare draft against each A-layer item**. On a hit:
   [原步骤 3 的内容不变，仅编号顺延]

4. **开放式反查（清单之外，独立一步，仍一轮不循环）**：步骤 2-3 的清单核对完成后，**丢开清单**，把五字段原文和当前草稿表**并排重读一遍**，切到「拿这份表去找茬的评审」立场，只问自己一句：**"如果这份测试点表被拿去让人挑漏洞，最可能被挑出来的是哪个方向？"**——只问这一句、想到即记，**不追加第二轮、不为了凑数而想**。命中的方向按步骤 3 同样规则分流（方向明确→补行；具体值/实现不明→存疑）；命中不到就此停止，不勉强找。**本步骤仍受红线 0**，不改变「一轮不循环」的纪律——它是清单核对之外的**第二个输入源**，不是清单的第二轮。

5. **Hard rules** (non-negotiable): [原步骤 4 内容不变，仅编号顺延]

6. **Scope**: [原步骤 5 内容不变，仅编号顺延]
```

**为什么这么改、解决哪条机制**：核实确认（诊断机制 3「自查左手查右手」部分成立）——§6 现有输入源只有「review-checklist.md 12 条固定条目」，v2.3 §1.2 记录的七类真实人工 review 遗漏没有一条命中这 12 条清单，说明「固定清单比对」这个动作结构性地查不出业务专属漏测。新增步骤给自查换了一个**独立于清单的输入源**——直接回到需求原文和草稿本身找漏，模拟外部查漏 AI 的工作方式；但保留「一轮、不循环、红线 0 不变」这些既有纪律，避免自查退化成第二次生成、避免与 v2.2/v2.3 已确立的「输入边界钉死」原则冲突。

**交叉引用影响**：

1. **§6 内部步骤编号**：原步骤 3（Compare against A-layer）不变仍是 3；原步骤 4（Hard rules）→ 5；原步骤 5（Scope）→ 6。新步骤占号 4。
2. **正文交叉引用检查**（已用 `grep -n "§6 步骤\|§6 step"` 核实，见下方核实记录）：**全文没有任何地方引用「§6 步骤 N」这种细粒度编号**——Rules Index（第 642 行）只写「Self-critique → §6」，不细到步骤号；`Skip when §5 step 8 triggered`（第 314 行）引用的是 §5 的 step 8，与本次改动的 §6 内部编号无关，不受影响。
3. **Walkthrough 示例**：三个 walkthrough 示例（第 576-629 行）均未提及 §6 自查的具体步骤号，不受影响。
4. **§9 相关引用**（第 331 行「Self-critique补 rows are AI-generated → is_edited: false」）：这句话是描述性的，不带步骤号，不受影响。

**结论：本条改动是四条中唯一涉及步骤重排的，但重排范围严格限定在 §6 内部（3 个步骤号顺延 1 位），且已核实全文没有任何外部引用依赖这些编号，交叉影响面为零。**

**该条独立验证方法**：

1. 抽 v2.3 §1.2 记录的那批人工 review 曾指出遗漏的原始需求，改动前后各跑一次生成，检查这一步「开放式反查」是否能命中其中部分方向（这批遗漏已确认不在 review-checklist 清单范围内，是检验「新输入源是否真的带来新增量」的最直接样本）。
2. 检查输出的存疑清单/测试点表行数变化幅度——预期为「小幅增加」，如果暴涨则说明「只问一句、想到即记」的约束没被遵守，此步骤在退化成第二次生成。
3. 检查这一步补充的内容是否仍然满足红线 0（不含编造的具体值/文案/阈值）。

**若验证不过、如何单独回退**：`git revert` 对应 commit，§6 步骤编号回退到原状（3/4/5），不影响 §5 或其他章节。因为本条改动与 P0/P1/P3 分别修改不同段落、无共享上下文，可独立回退不牵连其他三条。

---

### P3 — closure/存疑纪律补「方向唯一但未拍板 → 转生成」通道

**改哪个文件的哪一段**：`skills/cawplan-testpoint-generate/SKILL.md`，§5「存疑清单纪律」段落，第 254 行之后（第 250-256 行区块内）。

**当前内容**（第 250-256 行）：
```
250  **存疑清单纪律** (§5 — applies to step 2 partition accountability, step 5 closure, and §7 presentation):
251
252  - Format: 〔指向哪〕+〔为什么疑〕+〔建议动作〕.
253  - **Forbidden sole triggers**: `素材未提及` / `需补充` (A1 requirement-gap phrasing — A2 covers gaps with test-point rows or scope/ambiguity 存疑, not A1 copy).
254  - **Unsure unique vs ambiguous** (implementation style, stretch scope) → lean **存疑**; do not skip B-class confirmation to seem helpful. **Unsure whether `异常` baseline applies** (e.g. wrong credentials on credential login) → follow checklist section 3 + step 1 / (a), not 存疑.
255  - When unsure whether to generate a **directional** baseline row vs 存疑 → **红线 0** first: if specifics would be invented, stop that part; if only directional, generate.
256
```

**before**（第 254 行）：
```
- **Unsure unique vs ambiguous** (implementation style, stretch scope) → lean **存疑**; do not skip B-class confirmation to seem helpful. **Unsure whether `异常` baseline applies** (e.g. wrong credentials on credential login) → follow checklist section 3 + step 1 / (a), not 存疑.
```

**after**（第 254 行不变，紧接其后新增一行）：
```
- **Unsure unique vs ambiguous** (implementation style, stretch scope) → lean **存疑**; do not skip B-class confirmation to seem helpful. **Unsure whether `异常` baseline applies** (e.g. wrong credentials on credential login) → follow checklist section 3 + step 1 / (a), not 存疑.
- **方向唯一、只是「要不要测」未定**（功能形态上适用某轴/某分区，但五字段既未确认也未排除，且不涉及「往哪个方向写」的歧义）→ **生成 1 条方向性代表行**（红线 0 约束不变，不含任何具体值/文案/阈值/错误码），标题末尾加 `（范围待确认）`；**不转存疑**。**与「多种合理实现方式」严格区分**：后者是「往哪个方向写都可能猜错」（如菜单隐藏 vs 点击报错，两种实现都合理）→ 仍然存疑；前者是「方向没有歧义，只是没人拍板测不测」（如功能形态判该测某个角色权限差异，但五字段没提也没排除）→ 转生成。判断不清归为「多种实现方式」时优先存疑，不得为了少写存疑而滥用本条。
```

**为什么这么改、解决哪条机制**：核实确认（诊断机制 1「红线 0 把该测的点推进存疑清单」部分成立）——现状「拿不准 → 存疑清单」是一个粗粒度的二元判断，把「方向本身有歧义」（该存疑）和「方向没有歧义、只是没人明确拍板要不要测」（可以直接生成方向性行）混在一起处理。红线 0 本身只拦「需要具体值」的部分，不拦「方向性断言」，所以这类「方向唯一但未拍板」的分区完全可以在不违反红线 0 的前提下转成正式测试点，只是历史上被笼统地划进了「拿不准→存疑」的默认出口。这条改动把这两类情况拆开，只放行「方向唯一」的那一半，「多种实现方式」仍然按现状存疑处理，边界清晰、可判定。

**交叉引用影响**：

1. **不涉及步骤重排**——这是在已有段落内新增一条并列的判断规则，不新增/删除任何编号步骤。
2. **需要检查是否与 closure (a)2 冲突**（第 219 行 `Multiple reasonable implementations → 存疑`）：不冲突，两者管辖范围互斥——(a)2 管「多种合理实现方式」（本条明确排除这类）；本条管「方向唯一、未拍板」。已在 after 文本里写明区分标准，避免执行时误用。
3. **需要检查是否与 closure (c) 的「已展开为分区的并入标题」出口（第 229/231 行）重复**：不重复——closure (c) 管的是「同轴关键取值收口」（已适用轴上的取值级检查，如「已测 A，B/C 未覆盖」），触发条件是「axis 已适用」；本条管的是「存疑清单纪律」层面的更早判断（决定一个分区/方向要不要进存疑清单），发生在 closure (c) 之前的判断链路上。两者是不同层级的机制，不重复，但**需要在 (c) 段落末尾加一句避免误读**（见下）。
4. **建议同步微调**（非必需，但为避免歧义）：第 231 行 closure (c) 的 `Bounds` 段落末尾可加一句「与存疑清单纪律新增的『方向唯一未拍板』通道分工：(c) 管同轴取值级收口，前者管分区/方向级的存疑转生成，不重复触发」。**本轮先不做此项微调，观察 P3 独立验证结果后再决定是否需要**——避免一次改动同时触及两个段落，违反「改一条验一条」的纪律。

**该条独立验证方法**：

1. 抽 5-8 个真实需求，标记出「方向唯一未拍板」类型的分区（人工在生成前预判哪些方向属于这一类），检查改动后这些分区是否确实转成了带 `（范围待确认）` 的正式行，而不是仍然进存疑清单。
2. 检查这些新生成行是否严格不含任何具体值/文案/阈值/错误码（红线 0 合规性抽查）。
3. 检查「多种合理实现方式」类存疑（closure (a)2）和「C/D 轴默认兜底」类存疑数量是否**没有**跟着减少——如果减少，说明本条被滥用去消化本该保留在存疑里的歧义类判断，需要收紧措辞。
4. 单需求生成总行数环比检查：不应出现数量级跳变（如从 10 条变 50 条）。

**若验证不过、如何单独回退**：`git revert` 对应 commit，第 254 行后新增的一行整体删除，不影响其他任何段落——因为这条改动是纯新增（不修改 254 行本身，只追加新行），回退等于精确删除新增内容，无残留悬空引用。若观察到「数量失控」或「多种实现方式存疑被挤占」，**优先回退本条**（见下方整体验证方案的「数量失控红线」）。

---

## 三、不可越界的红线

1. **红线 0 一个字不动**。四条改动全部作用在「要不要生成 / 要不要多写一句判断理由」这一层，不触碰「生成的内容里能不能带具体数字/文案/阈值/错误码」这一层。P0 只加判断前的强制书写、不涉及内容；P1 是核对不改动；P2 新增的反查步骤明文写了「仍受红线 0」；P3 新增的转生成通道明文写了「不含任何具体值/文案/阈值/错误码」，且严格区分「方向唯一」与「多种实现方式」，后者仍然存疑，不因本条而被误转生成。
2. **不碰归档命令**。`cli/src/commands/qa-insights.ts`（`testpoints archive` / `testpoints reconcile`）与 `cli/src/lib/qa-insights/body-builders.ts`（`buildTestPointBatchBody`）已核实：字段白名单固定为 `title/tags/group/priority/is_edited`，存疑清单从未进入任何持久化结构，这是 skill 层的设计选择，不是命令层的 bug。本轮四条改动全部落在 `SKILL.md` 与 `references/*.md`，**不修改任何 `cli/` 下的代码文件**。

---

## 四、验证方案（整体）

沿用 v2.3 §9 已建立的指标框架（漏测命中率 / 新增冗余率 / 存疑清单质量 / 未命中归因分类 / 下游执行质量对比 / A2 行数膨胀率），本轮补两项：

1. **具名跳过检查**（P0 专属，按 §2 P0 节的核实结论——不假设内部工作稿可事后调取）：主要验证手段是核对 §7 呈现给 SQA 的输出（分节测试点表 + 存疑清单）里没有出现轴判断理由的文字；若当次会话的推理过程恰好可见，再抽样核对是否真的带了原因，作为辅助验证。
2. **同批需求改前/改后对比**：用同一批需求（优先选 v2.3 §1.2 记录的七类遗漏对应的原始需求 + 新挑 3-5 个），改前用当前基线跑一次，每叠加一条改动后各跑一次（即 P0 后跑一次、P0+P2 后跑一次、P0+P2+P3 后跑一次——P1 不产生新版本，跳过），用同一个外部查漏 AI（或人工评审）对比：
   - 漏测命中数是否下降；
   - 新增行是否集中在预期类别（P0 对应「A/B 轴盲区暴露」、P2 对应「清单外的业务专属漏测」、P3 对应「方向唯一未拍板的分区」）；
   - 存疑清单行数变化。

**误伤红线**：若「多种合理实现方式」（closure a2）与「C/D 轴默认兜底」这两类存疑的数量跟着下降，说明红线 0 或既有存疑机制被误伤，**须回退对应改动**（优先怀疑 P3，其判据最贴近这条边界）。

**数量失控红线**：单需求生成行数不应出现数量级跳变（如 10 条 → 50 条）。若出现，优先怀疑 P3 判据被滥用（「方向唯一」被过度宽泛地认定），先回退 P3，观察一轮，再决定 P2 是否需要同步收紧。

---

## 五、实施顺序与依赖

**四条改动彼此低耦合，理由**：

- P0 只改 §5 三态判断的措辞（6 处），不新增编号、不影响其他章节。
- P1 不改任何文件。
- P2 只改 §6 内部（新增 1 步 + 3 个步骤号顺延），已核实全文无外部引用依赖这些编号，交叉面为零。
- P3 只在 §5「存疑清单纪律」段落新增 1 行，不修改既有行、不新增编号。

四条**没有相互依赖关系**——P0 不依赖 P2/P3 才能生效，P2 的「开放式反查」补的行走既有分流规则（与 P3 新增通道是并列关系，不是前置关系），P3 的判据独立于 P0 的具名跳过机制。**可以按任意顺序实施**，但建议顺序仍是 **P0 → P1（核对，不改） → P2 → P3**：

1. **先做 P0**：风险最低、改动最小、验证最快出结果（只需检查判断理由是否被要求写出、是否误泄漏到 SQA 可见输出），可以最快建立「改一条验一条」的节奏和信心。
2. **P1 顺带做**：不改文件，就是把核对证据写进本文档，不占用额外的验证窗口。
3. **P2 次之**：涉及自查行为变化，需要观察输出是否膨胀，验证窗口比 P0 长，且 P2 补的行本身可能与 P3 想解决的「方向唯一未拍板」类分区有重叠命中（两者都可能在同一个需求上补出同一条方向），**先做 P2、观察一轮后再做 P3**，能更干净地区分是哪一条改动带来了增量，避免像 v2.1 §0.2 论证过的「同时改多处导致归因不可拆解」的错误重演。
4. **P3 最后**：风险等级中等、最贴近红线 0 边界，且依赖 P2 的观察结果来判断「方向唯一未拍板」这类分区在实际需求里出现的频率是否符合预期（如果 P2 的开放式反查已经把这类分区大量转成了「补行」而非「存疑」，P3 要解决的问题可能已经被 P2 部分覆盖，需要重新评估 P3 的净增量）。

**每条之间建议至少跑完一轮 A/B 验证（第四节的同批需求对比）后才叠加下一条**，不要连续 commit 多条改动而不验证。

---

## 六、坦诚结论

- 四条改动经过本轮在当前基线上的重新核实，**没有发现应该放弃的一条**，但 P1 从「待做的改动」变成了「已完成的核对」，这不是凑数，是实际核实结果——v2.3 §6.4 确实已经把「宁少不宁多」类措辞处理干净了，本轮没有遗漏可补。
- P3 是四条里边界最需要谨慎、最容易被滥用的一条——「方向唯一 vs 多种实现方式」这个判断本身仍然依赖 AI 的主观判断，不是一个能写成硬规则的机械标准。这条改动降低的是「因为没有转生成通道而默认存疑」这个结构性偏置，但不能保证 AI 在每个具体案例上都精确站在边界正确的一侧——这是为什么第四节把 P3 列为唯一需要「数量失控红线」和「误伤红线」双重看护的一条，且实施顺序上放在最后、依赖前序观察结果。
- P0 与 v2.3 §5 step 2「分区归属检查」的关系需要说明清楚，避免读者误以为两者重复：分区归属检查堵的是「P1/P2 展开出的分区不能既不在标题也不在存疑地消失」，管辖对象是**分区**；P0 堵的是「A/B 轴本身判不适用时没有强制说明理由」，管辖对象是**轴**。轴判不适用时根本不会产生分区（v2.3 §5 step 2 第 213 行已明确「已判定不适用的轴不会产生分区，无需本条介入」），所以这两个机制作用在流程的不同节点，不重复、不冲突。
- **P0 的「留痕」卖点已在动手前被核实证伪，改用「强制书写」作为其真实机制**——`SKILL.md` 第 566 行 `Session state (in-conversation only)` 列出的三类状态里没有六槎位/轴判断理由这类内部工作稿，全文也没有任何地方定义它是可持久化、可事后调取的实体。这不影响 P0 是否值得做（强制多写一句理由本身仍有减少拍脑袋误判的价值），但影响了验证方法的设计——原计划里「检查内部工作稿是否带原因」这条不能作为主要验证手段，已改为「核对 SQA 可见输出没有泄漏」为主、「若推理过程恰好可见则抽样核对」为辅。这提醒了一件更通用的事：本方案里任何提到「内部工作稿」「内部记录」的机制（六槎位、P1/P2 分区、closure 判断痕迹），都应假定其不可持久化、不可事后审计，设计验证方法时不能依赖这个假设。
- **待评估、本轮不做**：P2（自查开放式反查的分流）与 P3（存疑纪律新增通道）用的是同一条核心判断——「方向明确→生成方向性行；方向有歧义（多种合理实现方式）→存疑」——这条判断在 P2 的新增步骤 4 和 P3 的新增行里各写了一遍。两处措辞目前一致，但分别维护，日后若只改一处会产生不一致。建议在 P0-P3 全部落地并观察稳定后，评估能否把这条判断收敛为一处权威定义（例如放在「存疑清单纪律」段落，§6 新增步骤改为引用而不是重复表述），本轮不动，避免同时处理五件事、打乱「改一条验一条」的节奏。
- **待处理、本轮不做**：最初诊断的机制 4「规则过载」（§5 生成逻辑仅占全文约 20%，近一半篇幅是入口决策树/呈现格式/归档确认这类流程性规则）本轮完全没有处理，且 P2 新增一步、P3 新增一行，客观上让篇幅略微增加而不是减少。这不构成本轮方案的缺陷——瘦身本来就不在这四条的范围内——但需要明确记录：**本轮四条改动只解决机制 1/2/3，机制 4 仍待解决，且需要在这四条落地稳定、观察窗口结束后，单独开一轮「删繁就简」**，内容包括合并 P2/P3 可能收敛的重复判断、砍掉冗余表述、压缩流程性规则的篇幅。这一轮瘦身应与当前四条改动分开处理，同样遵守「一次改一类、可独立归因」的纪律，不要在四条尚未观察稳定时提前合并处理。
