{
  "name": "flow-cawplan-skill",
  "version": "0.1.0",
  "private": true,
  "description": "CawPlan CLI and Agent Skill definitions",
  "workspaces": [
    "cli"
  ],
  "scripts": {
    "build": "npm run build --workspace=cli",
    "test": "npm test --workspace=cli",
    "cli": "npm run cli --workspace=cli --",
    "validate:skills": "bash scripts/validate-skills.sh"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Ubiquiti-UID/flow-cawplan-skill.git"
  },
  "engines": {
    "node": ">=20"
  }
}
